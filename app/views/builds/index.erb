<h1>Builds</h1>
<a href="/builds/new">Share your Build</a>
<% @builds.each do |build| %>
  <h2><%= build.title %></h2>
  <p><%= build.specs %></p>
  <img src="<%= build.image %>" alt="build">
<% end %>
